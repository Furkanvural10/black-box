ShakeLog — Network Interceptor for iOS
A lightweight, zero-configuration network monitoring library for iOS. Intercept, log, and display all HTTP traffic in your app — without touching a single line of your existing networking code.
